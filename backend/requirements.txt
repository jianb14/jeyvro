# JEYVRO backend dependencies (approved by owner per PROJECT_CONTEXT C3)
Django>=5.1,<6.0
djangorestframework>=3.15,<4.0
psycopg2-binary>=2.9
python-dotenv>=1.0
django-cors-headers>=4.4

# Testing (deferred from Phase 1 — lives here with the Django scaffold)
pytest>=8.0
pytest-django>=4.8
